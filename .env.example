# Option A: single connection URL (used on Render)
DATABASE_URL=postgresql://user:password@host/dbname

# Option B: individual vars (used for local Docker)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=energy_ingestion

DB_SSL=false
PORT=3000
